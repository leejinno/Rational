(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
1/2
1/2
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
(Initializing the two Rational numbers.
$Clear up the two Rational numbers.
